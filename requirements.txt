# Dépendances de développement — KadiPy
#
# Ce fichier est un alias vers pyproject.toml, qui est la source de vérité
# pour toutes les dépendances du projet.
#
# Pour un environnement de développement complet :
#   pip install -e ".[dev]"
#
# Pour ajouter la lecture des anciens fichiers .xls (format OLE) :
#   pip install -e ".[dev,xls]"
#
# Ne pas modifier ce fichier directement. Toute nouvelle dépendance
# doit être ajoutée dans pyproject.toml.

-e ".[dev]"